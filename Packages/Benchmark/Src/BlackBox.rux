/// An optimization barrier that forces the compiler to treat `val` as observable.
///
/// https://rux-lang.dev/docs/api/benchmark/black-box
pub func BlackBox<T>(val: T) -> T {
    var local = val;
    let ptr = @local;
    return *ptr;
}
