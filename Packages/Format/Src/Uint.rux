// The decimal text of a `uint`.

// `uint` is a type of its own rather than a name for `uint64`, as `int` is for `int64`.

import Core::{ Result, Unit };
import Text::{ Debug, Display, FormatError, FormatSpec, TextWriter };

extend uint : Display {
    /// Writes this value's digits.
    ///
    /// Allocates nothing: the digits go into a buffer on the stack and reach the writer in one call.
    ///
    /// # Failures
    /// Reports whatever the writer reported, stopping at the first refusal.
    ///
    /// @param writer the destination
    /// @param spec what the placeholder asked for
    /// @returns success when the complete rendering was accepted
    /// @see https://rux-lang.dev/docs/api/format/uint/write-display
    pub func WriteDisplay(self: *uint, writer: &var TextWriter, spec: FormatSpec) -> Result<Unit, FormatError> {
        return WriteIntegerSpec(writer, spec, *self as uint64, false);
    }
}

extend uint : Debug {
    /// The same digits.
    ///
    /// # Failures
    /// Reports whatever the writer reported, stopping at the first refusal.
    ///
    /// @param writer the destination
    /// @param spec what the placeholder asked for
    /// @returns success when the complete rendering was accepted
    /// @see https://rux-lang.dev/docs/api/format/uint/write-debug
    pub func WriteDebug(self: *uint, writer: &var TextWriter, spec: FormatSpec) -> Result<Unit, FormatError> {
        return self.WriteDisplay(writer, spec);
    }
}
