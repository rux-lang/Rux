// A string has no primitive constants: it is a view over code units rather than a value with a width, so
// string16::Bits and string16::Bytes would have to choose between describing the view and describing one code
// unit, and string16::Min and string16::Max describe neither. Its length is a property of the value, not of
// the type, and is read from the string itself.
