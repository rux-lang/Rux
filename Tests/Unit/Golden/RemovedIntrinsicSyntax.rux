// The attribute the `intrinsic` keyword replaced. The `$` sigil is caught by the
// lexer, so it is kept on its own case in RemovedDollarSigil.
struct Target { pointerBits: uint; }

#Intrinsic("Assert")
func Assert(condition: bool, message: Slice<char8>);

func Main() -> int {
    return 0;
}
