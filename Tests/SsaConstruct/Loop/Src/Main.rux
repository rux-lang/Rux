// SSA test: while loop — phi nodes required at loop header for
// both the counter `i` and accumulator `sum`.
func Main() -> int {
    var i: int = 0;
    var sum: int = 0;
    while i < 5 {
        sum = sum + i;
        i = i + 1;
    }
    return sum; // 0+1+2+3+4 = 10
}
