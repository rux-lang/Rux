import Io::{
    Print,
    PrintLine,
    ReadLine
};

func EchoLine() {
    let mut line = ReadLine();
    Print("[");
    Print(line);
    PrintLine("]");
    line.Free();
}

func Main() -> int {
    EchoLine();
    EchoLine();
    EchoLine();
    EchoLine();
    return 0;
}
