// The commented-out body below imports Rux::{ Assert, #config }; both are left
// out for now so the placeholder does not trip the unused-import lint.

func Main() -> int {
    // TODO: Config's whole surface is the Config.Has and Config.Get intrinsics,
    // and neither is implemented in the compiler yet -- calling either crashes
    // at load with STATUS_ENTRYPOINT_NOT_FOUND (0xC0000139). Re-enable the body
    // below once the intrinsics link. Until then this package builds and passes
    // as a placeholder so the coverage stays wired into the suite.
    //
    // // This test package defines no configuration values, so an arbitrary name
    // // is absent and its lookup comes back empty.
    // Assert(!#config.Has("UNDEFINED_CONFIG_NAME"), "undefined name is absent");
    // Assert(#config.Get("UNDEFINED_CONFIG_NAME").length == 0, "undefined name is empty");
    //
    // // Has and Get agree with each other: a name Has reports absent has nothing
    // // for Get to return.
    // let name = "ANOTHER_UNDEFINED_NAME";
    // Assert(!#config.Has(name), "second undefined name is absent");
    // Assert(#config.Get(name).length == 0, "second undefined name is empty");
    return 0;
}
