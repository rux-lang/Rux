import Rux::Assert;

struct Point {
    x: float;
    y: float;
    z: float;
}

func Main() -> int {
    let mut point = Point {x: 10.0, y: 20.0, z: 30.0};
    let mut ptr = @point;
    Assert(ptr != null, "Pointer is null");
    return 0;
}
