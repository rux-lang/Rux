// bool256 is a reserved primitive type: the catalog names it, and this compiler does not implement it.
//
// This directory exists so the primitive matrix under Tests/Language is complete by inspection rather than
// by inference. Every entry in Compiler/Types/PrimitiveCatalog.cpp has a directory here, and
// LanguageTestMatrixTests checks that the two agree, so a width cannot reach the catalog without somewhere
// to exercise it.
//
// A program using bool256 cannot be that exercise, because it does not compile: the compiler reports that the
// type is reserved but not implemented in this version. A refusal is not something an executable test can
// assert, since one passes by exiting zero. It is asserted in Tests/Unit/Golden/ReservedPrimitiveTypes.rux
// instead, where an expected compile failure is the whole point.
//
// What is left to check here is the boundary: bool64 is the widest implemented boolean, and this
// file pins that it is the one carrying the constants. When bool256 is implemented, replace this placeholder
// with the real case rather than adding a directory.

import Core::Assert;
import Core::bool64;

func Main() -> int {
    Assert(bool64::Bits == 64u, "bool64 is the implemented width beside reserved bool256");
    Assert(bool64::Bytes * 8u == bool64::Bits, "its byte and bit counts agree");
    return 0;
}
