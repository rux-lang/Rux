import Rux::Assert;

func Main() -> int {
    var i = 0;

    do { i++; } while i < 50;
    Assert(i == 50, "Expected i to be 50 after do-while loop");

    while i < 100 { ++i; }
    Assert(i == 100, "Expected i to be 100 after while loop");

    return 0;
}
